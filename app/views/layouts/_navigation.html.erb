<nav class="navbar navbar-inverse" id="nav-navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
         <%=link_to( image_tag('PanoramaSur-01.png', :class=>"img-responsive", :id=>"logo-panorama"), root_path , :class=>"navbar-brand") %>
    </div>
    <div class="collapse navbar-collapse">
      <ul id="links-nav" class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <%= render 'layouts/navigation_user' %>
        <% elsif producer_signed_in? %>
          <%= render 'layouts/navigation_producer' %>
        <% else %>
          <%= render 'layouts/navigation_guess' %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
